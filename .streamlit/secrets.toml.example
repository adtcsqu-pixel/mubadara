# Keep this file as an example only. Do NOT commit a real token.
# In Streamlit Community Cloud, paste these values into App > Settings > Secrets.

[github]
token = "github_pat_xxxxxxxxxxxxxxxxx"
repo = "YOUR_ORG_OR_USERNAME/mubadara-data"
branch = "main"


# Change these before production deployment.
[auth]
admin_user = "admin"
admin_password = "Mubadara@2026"
# Keep true during testing to prevent accidental lockout.
# Set to false after you define your own production credentials.
allow_default_admin = true
